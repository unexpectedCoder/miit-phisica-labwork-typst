# Шаблон отчёта по лабораторной работе кафедры «Физика» РУТ (МИИТ)

Шаблон [Typst](https://typst.app/) отчёта по лабораторным работам по физике для студентов [РУТ (МИИТ)](https://www.miit.ru/).

> **Typst** — это современная альтернатива Microsoft Word и LaTeX для вёрстки научных документов.
> Вы пишете текст в специальном файле `.typ`, а программа автоматически формирует красиво оформленный PDF.

---

## Шаг 1. Установка программ

### Вариант А: VS Code (рекомендуется)

1. Установите [VS Code](https://code.visualstudio.com/) — бесплатный текстовый редактор.
2. Откройте VS Code и установите расширение **Tinymist Typst**:
   * Нажмите на иконку расширений на боковой панели (или `Ctrl+Shift+X`).
   * В строке поиска введите `Tinymist Typst`.
   * Нажмите **Install** (Установить).
   * Это расширение добавит поддержку Typst: подсветку синтаксиса и предпросмотр документа в реальном времени.
3. *(Необязательно)* Установите расширение [Typst Sync](https://marketplace.visualstudio.com/items?itemName=OrangeX4.vscode-typst-sync) для синхронизации с облаком.

### Вариант Б: онлайн-редактор typst.app

Если не хотите ничего устанавливать, воспользуйтесь официальным онлайн-редактором [typst.app](https://typst.app/).
Зарегистрируйтесь, создайте проект и загрузите в него файлы шаблона.

---

## Шаг 2. Установка шрифтов

> ⚠️ Этот шаг нужен **только при работе в VS Code**. В typst.app шрифты устанавливать не нужно.

Шаблон использует шрифты **STIX Two Text** (для текста) и **STIX Two Math** (для формул). Они бесплатные и свободно распространяются.

1. Скачайте архив со шрифтами [по этой ссылке](https://www.fonts.uprock.ru/fonts/stix).
2. Распакуйте архив.
3. Откройте папку `static_ttf` внутри архива.
4. Выделите все файлы с расширением `.ttf` (можно `Ctrl+A`).
5. Нажмите правой кнопкой мыши → **Установить** (или **Установить для всех пользователей**).
6. Если VS Code был открыт во время установки — **закройте и откройте его заново**, чтобы он увидел новые шрифты.

---

## Шаг 3. Скачивание шаблона

1. На странице репозитория нажмите зелёную кнопку **Code** → **Download ZIP**.
2. Распакуйте скачанный архив в удобную папку (например, `Документы/Физика/Лаба1`).

---

## Шаг 4. Открытие проекта

**В VS Code:**
* Откройте VS Code.
* Выберите `Файл → Открыть папку...` (или `File → Open Folder...`).
* Укажите папку, в которую распаковали архив.

**В typst.app:**
* Создайте новый проект.
* Загрузите все файлы из распакованного архива в проект.

---

## Шаг 5. Написание отчёта

Весь отчёт пишется в файле **[`lab.typ`](lab.typ)**. Остальные файлы трогать не нужно.

### Что делать в файле `lab.typ`:

1. **Заполните титульный лист** — найдите в начале файла поля, выделенные жёлтым (`highlight[...]`), и замените их на свои данные:
   * тема лабораторной работы,
   * обозначение работы (например, `Л-1`),
   * ФИО авторов,
   * группа, подгруппа, бригада,
   * ФИО преподавателя и лектора,
   * аннотация (краткое описание цели и результатов — заполняется в конце).

2. **Напишите текст отчёта** — следуйте структуре, уже заданной в файле:
   * Цель и задачи работы
   * Теоретическая часть (описание явления, модель, предсказание)
   * Практическая часть (измерения, обработка, расчёт, анализ, итог)
   * Заключение
   * Приложения (при необходимости)

3. **Удалите подсказки** — серые блоки ` ``` ... ``` ` в файле содержат методические подсказки. Перед сдачей отчёта их нужно удалить.

> 💡 **Предпросмотр в VS Code:** нажмите на иконку 👁 в правом верхнем углу редактора (или `Ctrl+Shift+P` → `Typst Preview`) — откроется панель с живым предпросмотром PDF.

---

## Шаг 6. Экспорт в PDF

**В VS Code:**
* Нажмите `Ctrl+Shift+P`, введите `Export` и выберите **Typst: Export PDF**.
* PDF сохранится рядом с файлом `lab.typ`.

**В typst.app:**
* Нажмите кнопку **Download PDF** (иконка скачивания) в правом верхнем углу.

---

## Структура проекта

| Файл / папка | Описание |
|---|---|
| `lab.typ` | **Основной файл отчёта — редактируете только его** |
| `labreport.typ` | Настройки шаблона (стили, отступы и т.д.) — не трогать |
| `лого.png` | Логотип РУТ (МИИТ) для титульного листа |
| `pics/` | Папка для ваших рисунков и изображений |

> **Файл `labreport.typ` редактировать не нужно.** Он содержит все настройки оформления и работает автоматически.

---

## Частые вопросы

**❓ Шрифты не отображаются / документ выглядит неправильно**
→ Убедитесь, что шрифты STIX Two установлены (Шаг 2), и перезапустите VS Code.

**❓ Не работает предпросмотр**
→ Убедитесь, что расширение Tinymist Typst установлено и активно.

**❓ Как добавить рисунок?**
→ Скопируйте файл изображения в папку `pics/`, затем используйте в тексте:
```typst
#figure(
  image("pics/имя_файла.png", width: 80%),
  caption: [Подпись к рисунку]
) <метка-рисунка>
```
Сослаться на рисунок в тексте: `@метка-рисунка`

**❓ Как написать формулу?**
→ Формула в строке: `$F = m a$`
→ Формула на отдельной строке:
```typst
$
F = m a
$
```
Больше примеров — прямо в файле `lab.typ`.

---

## Полезные ссылки

* 📖 [Документация Typst](https://typst.app/docs/) — официальная документация
* 🎓 [Урок по основам Typst](https://typst.app/docs/tutorial/) — быстрый старт за 30 минут
* 🔢 [Математические обозначения для физики](https://github.com/Leedehai/typst-physics/blob/master/physica-manual.pdf) — пакет physica
